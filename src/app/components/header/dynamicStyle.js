import {createStyles} from "@material-ui/core";
const style = createStyles({
    root: {
        flexGrow: 1,
    },
    menuButton: {
        marginRight: 2,
    },
    title: {
        flexGrow: 1,
    }
});
export default style
