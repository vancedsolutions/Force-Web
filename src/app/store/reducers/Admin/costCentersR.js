

import { GET_COST_CENTERS } from "../../types";

const initialState = {};
const costCentersR = (state = initialState, action) => {
    switch (action.type) {



        default: return state;
    }
}

export default costCentersR;